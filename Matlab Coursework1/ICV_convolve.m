function imgout = ICV_convolve(img, mask, matConst) %needs to work on a square matrix of size 3,5,7

%{
for 
    for
        for
            for
              
              
            end
        end
    end
end
%}
imgout = zeros(3,3);